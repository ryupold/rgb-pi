<phone:PhoneApplicationPage
    x:Class="RGB.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignData SampleData/MainViewModelSampleData.xaml}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--Pivot Control-->
        <phone:Pivot Title="RGB-Pi">
            <!--Pivot item: choose color-->
            <phone:PivotItem Header="choose color" Name="piChooseColor">
                <Grid Name="gridChooseColor"/>
            </phone:PivotItem>

            <!--Pivot item: fading-->
            <phone:PivotItem Header="commands" Name="piCommands" Margin="12,0">
                <Grid Name="gridCommands">
                    <Slider Name="slMinSpeed" SmallChange="1" Value="20" Minimum="10" Maximum="500" Margin="0,42,0,492" RenderTransformOrigin="0.49,0.512" HorizontalAlignment="Left" Width="456"/>
                    <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="min. speed" VerticalAlignment="Top"/>
                    <Slider Name="slMaxSpeed" SmallChange="1" Value="200" Minimum="10" Maximum="500" Margin="0,166,0,363" RenderTransformOrigin="0.493,0.49" HorizontalAlignment="Left" Width="456"/>
                    <TextBlock HorizontalAlignment="Left" Margin="10,139,0,0" TextWrapping="Wrap" Text="max. speed" VerticalAlignment="Top"/>

                    <Button Content="start fading" Margin="0,516,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.421,-0.132" Click="Button_Click"/>
                    <Slider x:Name="slMinBrightness" SmallChange="1" Value="10" Maximum="255" Margin="0,295,0,239" RenderTransformOrigin="0.493,0.49" HorizontalAlignment="Left" Width="456"/>
                    <TextBlock HorizontalAlignment="Left" Margin="10,268,0,0" TextWrapping="Wrap" Text="min. brightness" VerticalAlignment="Top"/>
                    <Slider x:Name="slMaxBrightness" SmallChange="1" Value="200" Maximum="255" Margin="0,419,0,115" RenderTransformOrigin="0.493,0.49" HorizontalAlignment="Left" Width="456"/>
                    <TextBlock HorizontalAlignment="Left" Margin="10,392,0,0" TextWrapping="Wrap" Text="max. brightness" VerticalAlignment="Top"/>
                </Grid>
            </phone:PivotItem>

            <phone:PivotItem Header="Dim" Name="piDim">
                <Grid Name="gridDim">
                    <Grid Name="gridDimColor" Margin="0,0,0,293"/>

                    <Button Content="Dim over 15 minutes" Margin="20,404,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.506" Name="btnDim" Click="btnDim_Click" Height="79"/>
                    <Slider  HorizontalAlignment="Left" Margin="10,315,0,0" VerticalAlignment="Top" Width="436" Name="slideDimTime" Minimum="1" SmallChange="1" Value="900" Maximum="7200" ValueChanged="slideDimTime_ValueChanged"/>

                </Grid>
            </phone:PivotItem>

            <phone:PivotItem Header="specials" Name="piSpecials">
                <Grid Name="gridSpecials">

                    <Button Content="Jamaica" Margin="10,10,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.633,0.535" Name="btnSpecialsJamaica" HorizontalAlignment="Left" Width="180" Click="btnSpecialsJamaica_Click"/>

                </Grid>
            </phone:PivotItem>

        </phone:Pivot>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>